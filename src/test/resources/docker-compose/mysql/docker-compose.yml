version: '3.8'

services:
  mysqldb-fasteats:
    container_name: mysqldb-fasteats
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: fasteatsdb
      MYSQL_USER: fiap56
      MYSQL_PASSWORD: fiapsoat1grupo56
      MYSQL_ROOT_PASSWORD: fiapsoat1grupo56
    networks:
      fasteats-net:
    ports:
      - '3306:3306'
    expose:
      - '3306'

networks:
  fasteats-net:
